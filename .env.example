# Assay environment variables
#
# Copy this file to .env (optional). Assay also reads these from your shell.

# Optional but recommended: improves Etherscan metadata + labels reliability.
ETHERSCAN_API_KEY=

# Proxy mode default upstream JSON-RPC URL.
ASSAY_UPSTREAM=

# Optional: path to a JSON config file (otherwise uses assay.config.json or ~/.config/assay/config.json).
ASSAY_CONFIG=

# Only needed if you run the embedded HTTP server (/v1/scan) in scripts/http-server.ts.
ASSAY_API_KEY=
